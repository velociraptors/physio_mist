#ifndef FUNCTIONS_H
#define	FUNCTIONS_H
#include <cstring>
/*Converts a string entirely to uppercase*/
char* convertToUpper(char *str);
char* insertSpaces(const char *str);

float convertCharToInt(char c);
double	convertStrToFloat(const char *str);



#endif